<Window x:Class="MED100.Views.PedirTextoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="460" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource Brush.FondoApp}">
  <Border Margin="24">
    <StackPanel>
      <TextBlock x:Name="Mensaje" Style="{DynamicResource Texto.Cuerpo}"
                 TextWrapping="Wrap" Margin="0,0,0,12" />
      <TextBox x:Name="CajaTexto" Style="{DynamicResource Input.Texto}"
               KeyDown="CajaTexto_KeyDown" />
      <TextBlock x:Name="Aviso" Text="Este campo es obligatorio."
                 Foreground="{DynamicResource Brush.Destructivo}"
                 Style="{DynamicResource Texto.Caption}"
                 Visibility="Collapsed" Margin="0,8,0,0" />

      <Grid Margin="0,20,0,0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="12" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Content="Cancelar" Style="{DynamicResource Boton.Secundario}"
                IsCancel="True" Click="BotonCancelar_Click" />
        <Button Grid.Column="2" Content="Aceptar" Style="{DynamicResource Boton.Primario}"
                IsDefault="True" Click="BotonAceptar_Click" />
      </Grid>
    </StackPanel>
  </Border>
</Window>
